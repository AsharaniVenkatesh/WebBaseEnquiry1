package com.sangamone.web.services;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Service;

import com.sangamone.web.model.Login;
import com.sangamone.web.repository.LoginRepository;

@Service
public class LoginServices {

	/*
	 * @Autowired private LoginRepository loginRepository;
	 * 
	 * public Login login(String lusername, String lpassword) { Login user =
	 * loginRepository.findByUsernameAndPassword(lusername, lpassword); return user;
	 * }
	 */
	  
	  
}
